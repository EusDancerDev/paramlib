# Core dependencies
numpy>=1.21.0
pandas>=1.3.0

# Interdependent packages (version constraints for PyPI publishing)
filewise>=3.11.0
pygenutils>=16.2.0

# Note: paramlib is a pure Python library with minimal dependencies
# The Git dependencies are for enhanced functionality and integration 
